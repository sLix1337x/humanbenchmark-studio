import { SequenceMemoryClient } from "@/components/sequence-memory/sequence-memory-client";

export default function SequenceMemoryPage() {
  return (
    <main className="page-shell-tight">
      <SequenceMemoryClient />
    </main>
  );
}
